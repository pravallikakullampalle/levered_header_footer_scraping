import React from 'react';

const HeaderCSSComponent = ({ headerCSS }) => {
    // Your code for rendering header CSS
    return (
        <div>
            <h2>Header CSS:</h2>
            <pre>{headerCSS}</pre>
        </div>
    );
};

export default HeaderCSSComponent;
